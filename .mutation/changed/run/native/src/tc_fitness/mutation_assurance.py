"""Real mutmut execution and fail-closed exact-candidate mutation admission."""

from __future__ import annotations

import argparse
import ast
import fnmatch
import json
import os
import shutil
import sys
from datetime import UTC, datetime
from importlib.metadata import PackageNotFoundError, version
from pathlib import Path
from tempfile import TemporaryDirectory
from typing import Any
from uuid import UUID, uuid4

from tc_fitness.check_contract_execution import candidate_identity, payload_digest
from tc_fitness.mutation_scope import (
    MutationError,
    archive_executables,
    archive_files,
    candidate_archives,
    definition_digest,
    digest_bytes,
    read_policy,
    select_scope,
)
from tc_fitness.runner import run_bounded_process

TOOL_VERSION = "3.6.0"
SCHEMA = "tc.fitness/mutation-receipt/v1"
MAX_LOG_BYTES = 2 * 1024 * 1024


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__json__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__json__mutmut)
def _json(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_orig(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_1(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = None
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_2(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key not in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_3(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError(None)
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_4(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("XXduplicate JSON field in mutation evidenceXX")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_5(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate json field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_6(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("DUPLICATE JSON FIELD IN MUTATION EVIDENCE")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_7(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = None
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_8(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = None
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_9(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(None, object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_10(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=None)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_11(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_12(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), )
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_13(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding=None), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_14(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="XXutf-8XX"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_15(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="UTF-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_16(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(None) from exc
    if not isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_17(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if isinstance(value, dict):
        raise MutationError("mutation output must be an object")
    return value


def x__json__mutmut_18(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError(None)
    return value


def x__json__mutmut_19(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("XXmutation output must be an objectXX")
    return value


def x__json__mutmut_20(path: Path) -> dict[str, Any]:
    def unique(pairs: list[tuple[str, Any]]) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, value in pairs:
            if key in result:
                raise MutationError("duplicate JSON field in mutation evidence")
            result[key] = value
        return result

    try:
        value = json.loads(path.read_text(encoding="utf-8"), object_pairs_hook=unique)
    except (OSError, ValueError) as exc:
        raise MutationError(f"missing or malformed mutation output: {path.name}") from exc
    if not isinstance(value, dict):
        raise MutationError("MUTATION OUTPUT MUST BE AN OBJECT")
    return value

mutants_x__json__mutmut['_mutmut_orig'] = x__json__mutmut_orig # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_1'] = x__json__mutmut_1 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_2'] = x__json__mutmut_2 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_3'] = x__json__mutmut_3 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_4'] = x__json__mutmut_4 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_5'] = x__json__mutmut_5 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_6'] = x__json__mutmut_6 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_7'] = x__json__mutmut_7 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_8'] = x__json__mutmut_8 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_9'] = x__json__mutmut_9 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_10'] = x__json__mutmut_10 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_11'] = x__json__mutmut_11 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_12'] = x__json__mutmut_12 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_13'] = x__json__mutmut_13 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_14'] = x__json__mutmut_14 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_15'] = x__json__mutmut_15 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_16'] = x__json__mutmut_16 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_17'] = x__json__mutmut_17 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_18'] = x__json__mutmut_18 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_19'] = x__json__mutmut_19 # type: ignore # mutmut generated
mutants_x__json__mutmut['x__json__mutmut_20'] = x__json__mutmut_20 # type: ignore # mutmut generated
mutants_x__write_json__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__write_json__mutmut)
def _write_json(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_orig(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_1(path: Path, value: object) -> None:
    with path.open(None, encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_2(path: Path, value: object) -> None:
    with path.open("x", encoding=None) as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_3(path: Path, value: object) -> None:
    with path.open(encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_4(path: Path, value: object) -> None:
    with path.open("x", ) as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_5(path: Path, value: object) -> None:
    with path.open("XXxXX", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_6(path: Path, value: object) -> None:
    with path.open("X", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_7(path: Path, value: object) -> None:
    with path.open("x", encoding="XXutf-8XX") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_8(path: Path, value: object) -> None:
    with path.open("x", encoding="UTF-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_9(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(None, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_10(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, None, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_11(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=None, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_12(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=None, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_13(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=None)
        stream.write("\n")


def x__write_json__mutmut_14(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_15(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, sort_keys=True, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_16(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_17(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_18(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, )
        stream.write("\n")


def x__write_json__mutmut_19(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=False, indent=2, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_20(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=3, allow_nan=False)
        stream.write("\n")


def x__write_json__mutmut_21(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=True)
        stream.write("\n")


def x__write_json__mutmut_22(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write(None)


def x__write_json__mutmut_23(path: Path, value: object) -> None:
    with path.open("x", encoding="utf-8") as stream:
        json.dump(value, stream, sort_keys=True, indent=2, allow_nan=False)
        stream.write("XX\nXX")

mutants_x__write_json__mutmut['_mutmut_orig'] = x__write_json__mutmut_orig # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_1'] = x__write_json__mutmut_1 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_2'] = x__write_json__mutmut_2 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_3'] = x__write_json__mutmut_3 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_4'] = x__write_json__mutmut_4 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_5'] = x__write_json__mutmut_5 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_6'] = x__write_json__mutmut_6 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_7'] = x__write_json__mutmut_7 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_8'] = x__write_json__mutmut_8 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_9'] = x__write_json__mutmut_9 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_10'] = x__write_json__mutmut_10 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_11'] = x__write_json__mutmut_11 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_12'] = x__write_json__mutmut_12 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_13'] = x__write_json__mutmut_13 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_14'] = x__write_json__mutmut_14 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_15'] = x__write_json__mutmut_15 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_16'] = x__write_json__mutmut_16 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_17'] = x__write_json__mutmut_17 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_18'] = x__write_json__mutmut_18 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_19'] = x__write_json__mutmut_19 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_20'] = x__write_json__mutmut_20 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_21'] = x__write_json__mutmut_21 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_22'] = x__write_json__mutmut_22 # type: ignore # mutmut generated
mutants_x__write_json__mutmut['x__write_json__mutmut_23'] = x__write_json__mutmut_23 # type: ignore # mutmut generated
mutants_x__inputs__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__inputs__mutmut)
def _inputs(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_orig(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_1(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = None
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_2(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(None, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_3(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, None, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_4(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, None)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_5(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_6(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_7(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, )
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_8(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = None
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_9(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(None)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_10(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = None
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_11(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(None)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_12(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = None
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_13(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(None, head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_14(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), None, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_15(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, None, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_16(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=None)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_17(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_18(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_19(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_20(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, )
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_21(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(None), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_22(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = None
    return binding, head_files, policy


def x__inputs__mutmut_23(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "XXbaseXX": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_24(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "BASE": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_25(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "XXheadXX": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_26(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "HEAD": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_27(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "XXbase_source_digestXX": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_28(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "BASE_SOURCE_DIGEST": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_29(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(None),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_30(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "XXsource_digestXX": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_31(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "SOURCE_DIGEST": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_32(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(None),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_33(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "XXexecutable_pathsXX": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_34(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "EXECUTABLE_PATHS": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_35(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(None),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_36(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "XXscopeXX": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_37(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "SCOPE": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_38(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "XXengineXX": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_39(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "ENGINE": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_40(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "XXtoolXX": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_41(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "TOOL": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_42(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"XXnameXX": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_43(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"NAME": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_44(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "XXmutmutXX", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_45(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "MUTMUT", "version": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_46(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "XXversionXX": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_47(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "VERSION": TOOL_VERSION},
        "test_tiers": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_48(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "XXtest_tiersXX": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_49(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "TEST_TIERS": ["unit", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_50(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["XXunitXX", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_51(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["UNIT", "contract"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_52(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "XXcontractXX"],
    }
    return binding, head_files, policy


def x__inputs__mutmut_53(
    root: Path, base: str, head: str, broad: bool
) -> tuple[dict[str, Any], dict[str, bytes], dict[str, Any]]:
    before, after = candidate_archives(root, base, head)
    head_files = archive_files(after)
    policy = read_policy(head_files)
    scope = select_scope(archive_files(before), head_files, policy, broad=broad)
    binding = {
        "base": base,
        "head": head,
        "base_source_digest": digest_bytes(before),
        "source_digest": digest_bytes(after),
        "executable_paths": archive_executables(after),
        "scope": scope,
        "engine": candidate_identity(),
        "tool": {"name": "mutmut", "version": TOOL_VERSION},
        "test_tiers": ["unit", "CONTRACT"],
    }
    return binding, head_files, policy

mutants_x__inputs__mutmut['_mutmut_orig'] = x__inputs__mutmut_orig # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_1'] = x__inputs__mutmut_1 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_2'] = x__inputs__mutmut_2 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_3'] = x__inputs__mutmut_3 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_4'] = x__inputs__mutmut_4 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_5'] = x__inputs__mutmut_5 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_6'] = x__inputs__mutmut_6 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_7'] = x__inputs__mutmut_7 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_8'] = x__inputs__mutmut_8 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_9'] = x__inputs__mutmut_9 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_10'] = x__inputs__mutmut_10 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_11'] = x__inputs__mutmut_11 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_12'] = x__inputs__mutmut_12 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_13'] = x__inputs__mutmut_13 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_14'] = x__inputs__mutmut_14 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_15'] = x__inputs__mutmut_15 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_16'] = x__inputs__mutmut_16 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_17'] = x__inputs__mutmut_17 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_18'] = x__inputs__mutmut_18 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_19'] = x__inputs__mutmut_19 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_20'] = x__inputs__mutmut_20 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_21'] = x__inputs__mutmut_21 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_22'] = x__inputs__mutmut_22 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_23'] = x__inputs__mutmut_23 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_24'] = x__inputs__mutmut_24 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_25'] = x__inputs__mutmut_25 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_26'] = x__inputs__mutmut_26 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_27'] = x__inputs__mutmut_27 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_28'] = x__inputs__mutmut_28 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_29'] = x__inputs__mutmut_29 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_30'] = x__inputs__mutmut_30 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_31'] = x__inputs__mutmut_31 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_32'] = x__inputs__mutmut_32 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_33'] = x__inputs__mutmut_33 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_34'] = x__inputs__mutmut_34 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_35'] = x__inputs__mutmut_35 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_36'] = x__inputs__mutmut_36 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_37'] = x__inputs__mutmut_37 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_38'] = x__inputs__mutmut_38 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_39'] = x__inputs__mutmut_39 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_40'] = x__inputs__mutmut_40 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_41'] = x__inputs__mutmut_41 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_42'] = x__inputs__mutmut_42 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_43'] = x__inputs__mutmut_43 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_44'] = x__inputs__mutmut_44 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_45'] = x__inputs__mutmut_45 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_46'] = x__inputs__mutmut_46 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_47'] = x__inputs__mutmut_47 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_48'] = x__inputs__mutmut_48 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_49'] = x__inputs__mutmut_49 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_50'] = x__inputs__mutmut_50 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_51'] = x__inputs__mutmut_51 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_52'] = x__inputs__mutmut_52 # type: ignore # mutmut generated
mutants_x__inputs__mutmut['x__inputs__mutmut_53'] = x__inputs__mutmut_53 # type: ignore # mutmut generated
mutants_x__native_config__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__native_config__mutmut)
def _native_config(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_orig(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_1(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = None
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_2(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot * "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_3(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "XXpyproject.tomlXX"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_4(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "PYPROJECT.TOML"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_5(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = None
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_6(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else "XXXX"
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_7(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "XX[tool.mutmut]XX" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_8(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[TOOL.MUTMUT]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_9(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" not in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_10(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError(None)
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_11(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("XXmutation policy must own mutmut controls; a second configuration is forbiddenXX")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_12(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("MUTATION POLICY MUST OWN MUTMUT CONTROLS; A SECOND CONFIGURATION IS FORBIDDEN")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_13(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = None
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_14(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        None
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_15(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"]) + {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_16(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files} + set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_17(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split(None)[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_18(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("XX/XX")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_19(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[1] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_20(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(None)
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_21(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["XXsource_rootsXX"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_22(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["SOURCE_ROOTS"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_23(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"XXtestsXX", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_24(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"TESTS", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_25(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "XXtestXX", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_26(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "TEST", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_27(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "XXpyproject.tomlXX", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_28(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "PYPROJECT.TOML", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_29(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "XXsetup.cfgXX"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_30(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "SETUP.CFG"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_31(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = None
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_32(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "XXsource_pathsXX": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_33(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "SOURCE_PATHS": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_34(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["XXsource_rootsXX"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_35(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["SOURCE_ROOTS"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_36(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "XXpytest_add_cli_args_test_selectionXX": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_37(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "PYTEST_ADD_CLI_ARGS_TEST_SELECTION": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_38(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["XXtestsXX"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_39(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["TESTS"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_40(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "XXpytest_add_cli_argsXX": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_41(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "PYTEST_ADD_CLI_ARGS": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_42(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["XX-mXX", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_43(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-M", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_44(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "XXunit or contractXX"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_45(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "UNIT OR CONTRACT"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_46(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "XXalso_copyXX": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_47(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "ALSO_COPY": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_48(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "XXmax_stack_depthXX": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_49(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "MAX_STACK_DEPTH": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_50(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": +1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_51(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -2,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_52(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "XXmutate_only_covered_linesXX": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_53(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "MUTATE_ONLY_COVERED_LINES": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_54(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": True,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_55(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "XXtimeout_multiplierXX": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_56(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "TIMEOUT_MULTIPLIER": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_57(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 6,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_58(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "XXtimeout_constantXX": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_59(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "TIMEOUT_CONSTANT": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_60(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 2,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_61(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        None
    )


def x__native_config__mutmut_62(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n" - "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_63(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original - "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_64(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "XX\n[tool.mutmut]\nXX"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_65(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[TOOL.MUTMUT]\n"
        + "".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_66(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(None)
    )


def x__native_config__mutmut_67(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "XXXX".join(f"{key} = {json.dumps(value)}\n" for key, value in controls.items())
    )


def x__native_config__mutmut_68(snapshot: Path, files: dict[str, bytes], policy: dict[str, Any]) -> None:
    """Create authoritative tool controls in the disposable snapshot only."""
    project = snapshot / "pyproject.toml"
    original = project.read_text() if project.exists() else ""
    if "[tool.mutmut]" in original:
        raise MutationError("mutation policy must own mutmut controls; a second configuration is forbidden")
    copy_roots = sorted(
        {path.split("/")[0] for path in files}
        - set(policy["source_roots"])
        - {"tests", "test", "pyproject.toml", "setup.cfg"}
    )
    controls = {
        "source_paths": policy["source_roots"],
        "pytest_add_cli_args_test_selection": policy["tests"],
        "pytest_add_cli_args": ["-m", "unit or contract"],
        "also_copy": copy_roots,
        "max_stack_depth": -1,
        "mutate_only_covered_lines": False,
        "timeout_multiplier": 5,
        "timeout_constant": 1,
    }
    project.write_text(
        original
        + "\n[tool.mutmut]\n"
        + "".join(f"{key} = {json.dumps(None)}\n" for key, value in controls.items())
    )

mutants_x__native_config__mutmut['_mutmut_orig'] = x__native_config__mutmut_orig # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_1'] = x__native_config__mutmut_1 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_2'] = x__native_config__mutmut_2 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_3'] = x__native_config__mutmut_3 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_4'] = x__native_config__mutmut_4 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_5'] = x__native_config__mutmut_5 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_6'] = x__native_config__mutmut_6 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_7'] = x__native_config__mutmut_7 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_8'] = x__native_config__mutmut_8 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_9'] = x__native_config__mutmut_9 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_10'] = x__native_config__mutmut_10 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_11'] = x__native_config__mutmut_11 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_12'] = x__native_config__mutmut_12 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_13'] = x__native_config__mutmut_13 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_14'] = x__native_config__mutmut_14 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_15'] = x__native_config__mutmut_15 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_16'] = x__native_config__mutmut_16 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_17'] = x__native_config__mutmut_17 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_18'] = x__native_config__mutmut_18 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_19'] = x__native_config__mutmut_19 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_20'] = x__native_config__mutmut_20 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_21'] = x__native_config__mutmut_21 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_22'] = x__native_config__mutmut_22 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_23'] = x__native_config__mutmut_23 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_24'] = x__native_config__mutmut_24 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_25'] = x__native_config__mutmut_25 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_26'] = x__native_config__mutmut_26 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_27'] = x__native_config__mutmut_27 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_28'] = x__native_config__mutmut_28 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_29'] = x__native_config__mutmut_29 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_30'] = x__native_config__mutmut_30 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_31'] = x__native_config__mutmut_31 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_32'] = x__native_config__mutmut_32 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_33'] = x__native_config__mutmut_33 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_34'] = x__native_config__mutmut_34 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_35'] = x__native_config__mutmut_35 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_36'] = x__native_config__mutmut_36 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_37'] = x__native_config__mutmut_37 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_38'] = x__native_config__mutmut_38 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_39'] = x__native_config__mutmut_39 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_40'] = x__native_config__mutmut_40 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_41'] = x__native_config__mutmut_41 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_42'] = x__native_config__mutmut_42 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_43'] = x__native_config__mutmut_43 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_44'] = x__native_config__mutmut_44 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_45'] = x__native_config__mutmut_45 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_46'] = x__native_config__mutmut_46 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_47'] = x__native_config__mutmut_47 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_48'] = x__native_config__mutmut_48 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_49'] = x__native_config__mutmut_49 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_50'] = x__native_config__mutmut_50 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_51'] = x__native_config__mutmut_51 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_52'] = x__native_config__mutmut_52 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_53'] = x__native_config__mutmut_53 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_54'] = x__native_config__mutmut_54 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_55'] = x__native_config__mutmut_55 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_56'] = x__native_config__mutmut_56 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_57'] = x__native_config__mutmut_57 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_58'] = x__native_config__mutmut_58 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_59'] = x__native_config__mutmut_59 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_60'] = x__native_config__mutmut_60 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_61'] = x__native_config__mutmut_61 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_62'] = x__native_config__mutmut_62 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_63'] = x__native_config__mutmut_63 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_64'] = x__native_config__mutmut_64 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_65'] = x__native_config__mutmut_65 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_66'] = x__native_config__mutmut_66 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_67'] = x__native_config__mutmut_67 # type: ignore # mutmut generated
mutants_x__native_config__mutmut['x__native_config__mutmut_68'] = x__native_config__mutmut_68 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__native_mutants__mutmut)
def _native_mutants(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_orig(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_1(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = None
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_2(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = None
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_3(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = None
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_4(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(None)
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_5(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native * "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_6(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "XXmutmut-stats.jsonXX")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_7(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "MUTMUT-STATS.JSON")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_8(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = None
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_9(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get(None)
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_10(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("XXtests_by_mangled_function_nameXX")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_11(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("TESTS_BY_MANGLED_FUNCTION_NAME")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_12(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = None
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_13(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get(None)
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_14(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("XXduration_by_testXX")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_15(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("DURATION_BY_TEST")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_16(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict) and not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_17(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations and not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_18(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict) and not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_19(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_20(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_21(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_22(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_23(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError(None)
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_24(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("XXempty native test scope or missing test execution mapXX")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_25(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("EMPTY NATIVE TEST SCOPE OR MISSING TEST EXECUTION MAP")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_26(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["XXfunctionsXX"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_27(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["FUNCTIONS"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_28(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = None
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_29(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(None)
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_30(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native * (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_31(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] - ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_32(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["XXpathXX"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_33(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["PATH"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_34(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + "XX.metaXX"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_35(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".META"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_36(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = None
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_37(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get(None)
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_38(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("XXexit_code_by_keyXX")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_39(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("EXIT_CODE_BY_KEY")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_40(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_41(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError(None)
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_42(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("XXnative mutation result has no terminal status mapXX")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_43(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("NATIVE MUTATION RESULT HAS NO TERMINAL STATUS MAP")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_44(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = None
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_45(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(None, 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_46(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", None)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_47(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_48(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", )[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_49(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].split(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_50(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["XXselectorXX"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_51(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["SELECTOR"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_52(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit("XX.XX", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_53(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 2)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_54(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[1]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_55(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = None
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_56(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse(None)
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_57(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native * function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_58(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["XXpathXX"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_59(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["PATH"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_60(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError(None) from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_61(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("XXmissing or invalid generated mutant sourceXX") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_62(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("MISSING OR INVALID GENERATED MUTANT SOURCE") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_63(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = None
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_64(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." - node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_65(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module - "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_66(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "XX.XX" + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_67(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(None)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_68(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = None
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_69(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix(None)
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_70(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removeprefix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_71(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["XXselectorXX"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_72(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["SELECTOR"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_73(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("XX*XX")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_74(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = None
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_75(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) or name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_76(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(None) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_77(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = None
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_78(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(None, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_79(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, None)}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_80(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_81(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, )}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_82(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["XXselectorXX"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_83(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["SELECTOR"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_84(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory == reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_85(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError(None)
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_86(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("XXnative mutant inventory is missing or has unexpected result entriesXX")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_87(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("NATIVE MUTANT INVENTORY IS MISSING OR HAS UNEXPECTED RESULT ENTRIES")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_88(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = None
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_89(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(None)
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_90(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix - "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_91(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "XXorigXX")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_92(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "ORIG")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_93(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory or (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_94(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None and definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_95(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is not None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_96(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(None) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_97(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) == function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_98(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["XXdefinition_digestXX"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_99(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["DEFINITION_DIGEST"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_100(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError(None)
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_101(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("XXnative mutant source does not match the exact candidate definitionXX")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_102(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("NATIVE MUTANT SOURCE DOES NOT MATCH THE EXACT CANDIDATE DEFINITION")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_103(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = None
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_104(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(None)
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_105(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix(None))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_106(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removeprefix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_107(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("XX__mutmut_XX"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_108(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__MUTMUT_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_109(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory or (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_110(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests and not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_111(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list) and not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_112(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_113(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_114(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_115(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                None
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_116(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool) or durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_117(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float) or not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_118(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations or isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_119(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str) or test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_120(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test not in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_121(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_122(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] > 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_123(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 1
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_124(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError(None)
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_125(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("XXselected mutant has no real native test associationXX")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_126(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("SELECTED MUTANT HAS NO REAL NATIVE TEST ASSOCIATION")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_127(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(None):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_128(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_129(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(None, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_130(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, None):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_131(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_132(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, ):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_133(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["XXselectorXX"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_134(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["SELECTOR"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_135(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                break
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_136(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name not in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_137(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError(None)
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_138(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("XXduplicate native mutant identityXX")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_139(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("DUPLICATE NATIVE MUTANT IDENTITY")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_140(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(None) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_141(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) != function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_142(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["XXdefinition_digestXX"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_143(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["DEFINITION_DIGEST"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_144(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError(None)
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_145(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("XXnative result names an unchanged, no-op mutantXX")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_146(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("NATIVE RESULT NAMES AN UNCHANGED, NO-OP MUTANT")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_147(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(None)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_148(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(None) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_149(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_150(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = None
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_151(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "XXerrorXX"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_152(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "ERROR"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_153(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code != 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_154(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 2:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_155(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = None
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_156(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "XXkilledXX"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_157(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "KILLED"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_158(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code != 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_159(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 1:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_160(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = None
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_161(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "XXsurvivedXX"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_162(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "SURVIVED"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_163(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = None
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_164(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "XXerrorXX"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_165(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "ERROR"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_166(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                None
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_167(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "XXidXX": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_168(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "ID": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_169(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "XXfunctionXX": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_170(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "FUNCTION": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_171(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["XXnameXX"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_172(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["NAME"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_173(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "XXpathXX": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_174(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "PATH": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_175(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["XXpathXX"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_176(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["PATH"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_177(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "XXexit_codeXX": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_178(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "EXIT_CODE": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_179(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "XXstatusXX": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_180(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "STATUS": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_181(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_182(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError(None)
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_183(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("XXempty or zero-scope native mutation evidenceXX")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_184(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("EMPTY OR ZERO-SCOPE NATIVE MUTATION EVIDENCE")
    return sorted(mutants, key=lambda item: item["id"])


def x__native_mutants__mutmut_185(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(None, key=lambda item: item["id"])


def x__native_mutants__mutmut_186(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=None)


def x__native_mutants__mutmut_187(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(key=lambda item: item["id"])


def x__native_mutants__mutmut_188(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, )


def x__native_mutants__mutmut_189(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: None)


def x__native_mutants__mutmut_190(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["XXidXX"])


def x__native_mutants__mutmut_191(native: Path, scope: dict[str, Any]) -> list[dict[str, Any]]:
    mutants: list[dict[str, Any]] = []
    seen: set[str] = set()
    stats = _json(native / "mutmut-stats.json")
    associations = stats.get("tests_by_mangled_function_name")
    durations = stats.get("duration_by_test")
    if (
        not isinstance(associations, dict)
        or not associations
        or not isinstance(durations, dict)
        or not durations
    ):
        raise MutationError("empty native test scope or missing test execution map")
    for function in scope["functions"]:
        metadata = _json(native / (function["path"] + ".meta"))
        statuses = metadata.get("exit_code_by_key")
        if not isinstance(statuses, dict):
            raise MutationError("native mutation result has no terminal status map")
        module = function["selector"].rsplit(".", 1)[0]
        try:
            source = ast.parse((native / function["path"]).read_bytes())
        except (OSError, SyntaxError) as exc:
            raise MutationError("missing or invalid generated mutant source") from exc
        definitions = {
            module + "." + node.name: node
            for node in ast.walk(source)
            if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
        }
        prefix = function["selector"].removesuffix("*")
        inventory = {
            name for name in definitions if name.startswith(prefix) and name[len(prefix) :].isdigit()
        }
        reported = {name for name in statuses if fnmatch.fnmatchcase(name, function["selector"])}
        if inventory != reported:
            raise MutationError("native mutant inventory is missing or has unexpected result entries")
        original = definitions.get(prefix + "orig")
        if inventory and (original is None or definition_digest(original) != function["definition_digest"]):
            raise MutationError("native mutant source does not match the exact candidate definition")
        tests = associations.get(prefix.removesuffix("__mutmut_"))
        if inventory and (
            not isinstance(tests, list)
            or not tests
            or not all(
                isinstance(test, str)
                and test in durations
                and isinstance(durations[test], int | float)
                and not isinstance(durations[test], bool)
                and durations[test] >= 0
                for test in tests
            )
        ):
            raise MutationError("selected mutant has no real native test association")
        for name, code in sorted(statuses.items()):
            if not fnmatch.fnmatchcase(name, function["selector"]):
                continue
            if name in seen:
                raise MutationError("duplicate native mutant identity")
            if definition_digest(definitions[name]) == function["definition_digest"]:
                raise MutationError("native result names an unchanged, no-op mutant")
            seen.add(name)
            if type(code) is not int:
                status = "error"
            elif code == 1:
                status = "killed"
            elif code == 0:
                status = "survived"
            else:
                status = "error"
            mutants.append(
                {
                    "id": name,
                    "function": function["name"],
                    "path": function["path"],
                    "exit_code": code,
                    "status": status,
                }
            )
    if not mutants:
        raise MutationError("empty or zero-scope native mutation evidence")
    return sorted(mutants, key=lambda item: item["ID"])

mutants_x__native_mutants__mutmut['_mutmut_orig'] = x__native_mutants__mutmut_orig # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_1'] = x__native_mutants__mutmut_1 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_2'] = x__native_mutants__mutmut_2 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_3'] = x__native_mutants__mutmut_3 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_4'] = x__native_mutants__mutmut_4 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_5'] = x__native_mutants__mutmut_5 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_6'] = x__native_mutants__mutmut_6 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_7'] = x__native_mutants__mutmut_7 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_8'] = x__native_mutants__mutmut_8 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_9'] = x__native_mutants__mutmut_9 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_10'] = x__native_mutants__mutmut_10 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_11'] = x__native_mutants__mutmut_11 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_12'] = x__native_mutants__mutmut_12 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_13'] = x__native_mutants__mutmut_13 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_14'] = x__native_mutants__mutmut_14 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_15'] = x__native_mutants__mutmut_15 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_16'] = x__native_mutants__mutmut_16 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_17'] = x__native_mutants__mutmut_17 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_18'] = x__native_mutants__mutmut_18 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_19'] = x__native_mutants__mutmut_19 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_20'] = x__native_mutants__mutmut_20 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_21'] = x__native_mutants__mutmut_21 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_22'] = x__native_mutants__mutmut_22 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_23'] = x__native_mutants__mutmut_23 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_24'] = x__native_mutants__mutmut_24 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_25'] = x__native_mutants__mutmut_25 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_26'] = x__native_mutants__mutmut_26 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_27'] = x__native_mutants__mutmut_27 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_28'] = x__native_mutants__mutmut_28 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_29'] = x__native_mutants__mutmut_29 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_30'] = x__native_mutants__mutmut_30 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_31'] = x__native_mutants__mutmut_31 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_32'] = x__native_mutants__mutmut_32 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_33'] = x__native_mutants__mutmut_33 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_34'] = x__native_mutants__mutmut_34 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_35'] = x__native_mutants__mutmut_35 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_36'] = x__native_mutants__mutmut_36 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_37'] = x__native_mutants__mutmut_37 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_38'] = x__native_mutants__mutmut_38 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_39'] = x__native_mutants__mutmut_39 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_40'] = x__native_mutants__mutmut_40 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_41'] = x__native_mutants__mutmut_41 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_42'] = x__native_mutants__mutmut_42 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_43'] = x__native_mutants__mutmut_43 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_44'] = x__native_mutants__mutmut_44 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_45'] = x__native_mutants__mutmut_45 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_46'] = x__native_mutants__mutmut_46 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_47'] = x__native_mutants__mutmut_47 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_48'] = x__native_mutants__mutmut_48 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_49'] = x__native_mutants__mutmut_49 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_50'] = x__native_mutants__mutmut_50 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_51'] = x__native_mutants__mutmut_51 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_52'] = x__native_mutants__mutmut_52 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_53'] = x__native_mutants__mutmut_53 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_54'] = x__native_mutants__mutmut_54 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_55'] = x__native_mutants__mutmut_55 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_56'] = x__native_mutants__mutmut_56 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_57'] = x__native_mutants__mutmut_57 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_58'] = x__native_mutants__mutmut_58 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_59'] = x__native_mutants__mutmut_59 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_60'] = x__native_mutants__mutmut_60 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_61'] = x__native_mutants__mutmut_61 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_62'] = x__native_mutants__mutmut_62 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_63'] = x__native_mutants__mutmut_63 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_64'] = x__native_mutants__mutmut_64 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_65'] = x__native_mutants__mutmut_65 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_66'] = x__native_mutants__mutmut_66 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_67'] = x__native_mutants__mutmut_67 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_68'] = x__native_mutants__mutmut_68 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_69'] = x__native_mutants__mutmut_69 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_70'] = x__native_mutants__mutmut_70 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_71'] = x__native_mutants__mutmut_71 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_72'] = x__native_mutants__mutmut_72 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_73'] = x__native_mutants__mutmut_73 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_74'] = x__native_mutants__mutmut_74 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_75'] = x__native_mutants__mutmut_75 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_76'] = x__native_mutants__mutmut_76 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_77'] = x__native_mutants__mutmut_77 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_78'] = x__native_mutants__mutmut_78 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_79'] = x__native_mutants__mutmut_79 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_80'] = x__native_mutants__mutmut_80 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_81'] = x__native_mutants__mutmut_81 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_82'] = x__native_mutants__mutmut_82 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_83'] = x__native_mutants__mutmut_83 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_84'] = x__native_mutants__mutmut_84 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_85'] = x__native_mutants__mutmut_85 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_86'] = x__native_mutants__mutmut_86 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_87'] = x__native_mutants__mutmut_87 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_88'] = x__native_mutants__mutmut_88 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_89'] = x__native_mutants__mutmut_89 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_90'] = x__native_mutants__mutmut_90 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_91'] = x__native_mutants__mutmut_91 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_92'] = x__native_mutants__mutmut_92 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_93'] = x__native_mutants__mutmut_93 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_94'] = x__native_mutants__mutmut_94 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_95'] = x__native_mutants__mutmut_95 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_96'] = x__native_mutants__mutmut_96 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_97'] = x__native_mutants__mutmut_97 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_98'] = x__native_mutants__mutmut_98 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_99'] = x__native_mutants__mutmut_99 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_100'] = x__native_mutants__mutmut_100 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_101'] = x__native_mutants__mutmut_101 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_102'] = x__native_mutants__mutmut_102 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_103'] = x__native_mutants__mutmut_103 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_104'] = x__native_mutants__mutmut_104 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_105'] = x__native_mutants__mutmut_105 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_106'] = x__native_mutants__mutmut_106 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_107'] = x__native_mutants__mutmut_107 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_108'] = x__native_mutants__mutmut_108 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_109'] = x__native_mutants__mutmut_109 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_110'] = x__native_mutants__mutmut_110 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_111'] = x__native_mutants__mutmut_111 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_112'] = x__native_mutants__mutmut_112 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_113'] = x__native_mutants__mutmut_113 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_114'] = x__native_mutants__mutmut_114 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_115'] = x__native_mutants__mutmut_115 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_116'] = x__native_mutants__mutmut_116 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_117'] = x__native_mutants__mutmut_117 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_118'] = x__native_mutants__mutmut_118 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_119'] = x__native_mutants__mutmut_119 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_120'] = x__native_mutants__mutmut_120 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_121'] = x__native_mutants__mutmut_121 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_122'] = x__native_mutants__mutmut_122 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_123'] = x__native_mutants__mutmut_123 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_124'] = x__native_mutants__mutmut_124 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_125'] = x__native_mutants__mutmut_125 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_126'] = x__native_mutants__mutmut_126 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_127'] = x__native_mutants__mutmut_127 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_128'] = x__native_mutants__mutmut_128 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_129'] = x__native_mutants__mutmut_129 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_130'] = x__native_mutants__mutmut_130 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_131'] = x__native_mutants__mutmut_131 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_132'] = x__native_mutants__mutmut_132 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_133'] = x__native_mutants__mutmut_133 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_134'] = x__native_mutants__mutmut_134 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_135'] = x__native_mutants__mutmut_135 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_136'] = x__native_mutants__mutmut_136 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_137'] = x__native_mutants__mutmut_137 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_138'] = x__native_mutants__mutmut_138 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_139'] = x__native_mutants__mutmut_139 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_140'] = x__native_mutants__mutmut_140 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_141'] = x__native_mutants__mutmut_141 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_142'] = x__native_mutants__mutmut_142 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_143'] = x__native_mutants__mutmut_143 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_144'] = x__native_mutants__mutmut_144 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_145'] = x__native_mutants__mutmut_145 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_146'] = x__native_mutants__mutmut_146 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_147'] = x__native_mutants__mutmut_147 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_148'] = x__native_mutants__mutmut_148 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_149'] = x__native_mutants__mutmut_149 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_150'] = x__native_mutants__mutmut_150 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_151'] = x__native_mutants__mutmut_151 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_152'] = x__native_mutants__mutmut_152 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_153'] = x__native_mutants__mutmut_153 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_154'] = x__native_mutants__mutmut_154 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_155'] = x__native_mutants__mutmut_155 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_156'] = x__native_mutants__mutmut_156 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_157'] = x__native_mutants__mutmut_157 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_158'] = x__native_mutants__mutmut_158 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_159'] = x__native_mutants__mutmut_159 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_160'] = x__native_mutants__mutmut_160 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_161'] = x__native_mutants__mutmut_161 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_162'] = x__native_mutants__mutmut_162 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_163'] = x__native_mutants__mutmut_163 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_164'] = x__native_mutants__mutmut_164 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_165'] = x__native_mutants__mutmut_165 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_166'] = x__native_mutants__mutmut_166 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_167'] = x__native_mutants__mutmut_167 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_168'] = x__native_mutants__mutmut_168 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_169'] = x__native_mutants__mutmut_169 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_170'] = x__native_mutants__mutmut_170 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_171'] = x__native_mutants__mutmut_171 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_172'] = x__native_mutants__mutmut_172 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_173'] = x__native_mutants__mutmut_173 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_174'] = x__native_mutants__mutmut_174 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_175'] = x__native_mutants__mutmut_175 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_176'] = x__native_mutants__mutmut_176 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_177'] = x__native_mutants__mutmut_177 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_178'] = x__native_mutants__mutmut_178 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_179'] = x__native_mutants__mutmut_179 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_180'] = x__native_mutants__mutmut_180 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_181'] = x__native_mutants__mutmut_181 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_182'] = x__native_mutants__mutmut_182 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_183'] = x__native_mutants__mutmut_183 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_184'] = x__native_mutants__mutmut_184 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_185'] = x__native_mutants__mutmut_185 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_186'] = x__native_mutants__mutmut_186 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_187'] = x__native_mutants__mutmut_187 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_188'] = x__native_mutants__mutmut_188 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_189'] = x__native_mutants__mutmut_189 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_190'] = x__native_mutants__mutmut_190 # type: ignore # mutmut generated
mutants_x__native_mutants__mutmut['x__native_mutants__mutmut_191'] = x__native_mutants__mutmut_191 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__artifacts__mutmut)
def _artifacts(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_orig(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_1(output: Path) -> list[dict[str, str]]:
    artifacts = None
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_2(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(None):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_3(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob(None)):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_4(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("XX*XX")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_5(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" and not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_6(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path != output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_7(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output * "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_8(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "XXreceipt.jsonXX" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_9(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "RECEIPT.JSON" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_10(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_11(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            break
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_12(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError(None)
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_13(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("XXmutation artifacts must not be symlinksXX")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_14(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("MUTATION ARTIFACTS MUST NOT BE SYMLINKS")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_15(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            None
        )
    return artifacts


def x__artifacts__mutmut_16(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"XXpathXX": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_17(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"PATH": path.relative_to(output).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_18(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(None).as_posix(), "digest": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_19(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "XXdigestXX": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_20(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "DIGEST": digest_bytes(path.read_bytes())}
        )
    return artifacts


def x__artifacts__mutmut_21(output: Path) -> list[dict[str, str]]:
    artifacts = []
    for path in sorted(output.rglob("*")):
        if path == output / "receipt.json" or not path.is_file():
            continue
        if path.is_symlink():
            raise MutationError("mutation artifacts must not be symlinks")
        artifacts.append(
            {"path": path.relative_to(output).as_posix(), "digest": digest_bytes(None)}
        )
    return artifacts

mutants_x__artifacts__mutmut['_mutmut_orig'] = x__artifacts__mutmut_orig # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_1'] = x__artifacts__mutmut_1 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_2'] = x__artifacts__mutmut_2 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_3'] = x__artifacts__mutmut_3 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_4'] = x__artifacts__mutmut_4 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_5'] = x__artifacts__mutmut_5 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_6'] = x__artifacts__mutmut_6 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_7'] = x__artifacts__mutmut_7 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_8'] = x__artifacts__mutmut_8 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_9'] = x__artifacts__mutmut_9 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_10'] = x__artifacts__mutmut_10 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_11'] = x__artifacts__mutmut_11 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_12'] = x__artifacts__mutmut_12 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_13'] = x__artifacts__mutmut_13 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_14'] = x__artifacts__mutmut_14 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_15'] = x__artifacts__mutmut_15 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_16'] = x__artifacts__mutmut_16 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_17'] = x__artifacts__mutmut_17 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_18'] = x__artifacts__mutmut_18 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_19'] = x__artifacts__mutmut_19 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_20'] = x__artifacts__mutmut_20 # type: ignore # mutmut generated
mutants_x__artifacts__mutmut['x__artifacts__mutmut_21'] = x__artifacts__mutmut_21 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_execute_mutation__mutmut)
def execute_mutation(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_orig(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_1(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = True
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_2(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = None
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_3(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(None, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_4(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, None, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_5(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, None, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_6(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, None)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_7(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_8(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_9(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_10(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, )
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_11(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_12(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["XXscopeXX"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_13(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["SCOPE"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_14(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["XXfunctionsXX"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_15(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["FUNCTIONS"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_16(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            None, "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_17(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", None
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_18(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_19(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_20(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "XXno changed production function scope; no mutation receipt can be admittedXX", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_21(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "NO CHANGED PRODUCTION FUNCTION SCOPE; NO MUTATION RECEIPT CAN BE ADMITTED", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_22(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "XXzero-scopeXX"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_23(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "ZERO-SCOPE"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_24(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = None
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_25(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version(None)
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_26(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("XXmutmutXX")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_27(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("MUTMUT")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_28(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError(None, "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_29(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", None) from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_30(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_31(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", ) from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_32(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("XXrequired mutmut executable is unavailableXX", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_33(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("REQUIRED MUTMUT EXECUTABLE IS UNAVAILABLE", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_34(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "XXdependency-unavailableXX") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_35(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "DEPENDENCY-UNAVAILABLE") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_36(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = None
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_37(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name(None)
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_38(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(None).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_39(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("XXmutmutXX")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_40(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("MUTMUT")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_41(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION and not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_42(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version == TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_43(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_44(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError(None)
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_45(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("XXmutation execution requires the exact pinned mutmut toolXX")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_46(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("MUTATION EXECUTION REQUIRES THE EXACT PINNED MUTMUT TOOL")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_47(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError(None)
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_48(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("XXoutput already exists; retain the prior attempt and use a new directoryXX")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_49(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("OUTPUT ALREADY EXISTS; RETAIN THE PRIOR ATTEMPT AND USE A NEW DIRECTORY")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_50(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=None)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_51(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=False)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_52(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = None
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_53(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "XXschemaXX": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_54(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "SCHEMA": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_55(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "XXrun_idXX": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_56(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "RUN_ID": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_57(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "XXattemptXX": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_58(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "ATTEMPT": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_59(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "XXexecution_idXX": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_60(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "EXECUTION_ID": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_61(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(None),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_62(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "XXstarted_atXX": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_63(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "STARTED_AT": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_64(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(None).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_65(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "XXstatusXX": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_66(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "STATUS": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_67(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "XXerrorXX",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_68(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "ERROR",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_69(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "XXmutantsXX": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_70(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "MUTANTS": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_71(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix=None) as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_72(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="XXtc-mutation-XX") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_73(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="TC-MUTATION-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_74(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = None
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_75(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(None)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_76(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = None
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_77(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot * path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_78(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=None, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_79(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=None)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_80(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_81(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, )
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_82(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=False, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_83(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=False)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_84(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(None)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_85(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(None)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_86(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(494 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_87(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path not in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_88(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["XXexecutable_pathsXX"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_89(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["EXECUTABLE_PATHS"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_90(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 421)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_91(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(None, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_92(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, None, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_93(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, None)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_94(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_95(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_96(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, )
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_97(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes(None)
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_98(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output * "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_99(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "XXnative-config.tomlXX").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_100(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "NATIVE-CONFIG.TOML").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_101(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot * "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_102(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "XXpyproject.tomlXX").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_103(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "PYPROJECT.TOML").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_104(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = None
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_105(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(None),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_106(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "XXrunXX",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_107(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "RUN",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_108(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "XX--max-childrenXX",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_109(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--MAX-CHILDREN",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_110(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "XX2XX",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_111(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["XXselectorXX"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_112(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["SELECTOR"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_113(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["XXscopeXX"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_114(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["SCOPE"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_115(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["XXfunctionsXX"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_116(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["FUNCTIONS"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_117(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = None
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_118(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["XXcommandXX"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_119(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["COMMAND"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_120(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = None
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_121(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(None)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_122(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop(None, None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_123(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop(None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_124(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", )
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_125(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("XXPYTEST_ADDOPTSXX", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_126(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("pytest_addopts", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_127(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop(None, None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_128(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop(None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_129(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", )
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_130(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("XXPYTEST_PLUGINSXX", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_131(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("pytest_plugins", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_132(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop(None, None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_133(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop(None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_134(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", )
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_135(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("XXMUTANT_UNDER_TESTXX", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_136(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("mutant_under_test", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_137(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = None
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_138(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["XXPYTHONDONTWRITEBYTECODEXX"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_139(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["pythondontwritebytecode"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_140(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "XX1XX"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_141(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = None
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_142(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            None,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_143(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=None,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_144(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=None,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_145(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=None,
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_146(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=None,
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_147(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=None,
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_148(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_149(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_150(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_151(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_152(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_153(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_154(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["XXtimeout_secondsXX"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_155(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["TIMEOUT_SECONDS"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_156(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output * "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_157(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "XXstdout.logXX",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_158(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "STDOUT.LOG",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_159(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output * "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_160(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "XXstderr.logXX",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_161(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "STDERR.LOG",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_162(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = None
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_163(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["XXtool_exit_codeXX"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_164(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["TOOL_EXIT_CODE"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_165(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = None
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_166(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot * "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_167(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "XXmutantsXX"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_168(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "MUTANTS"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_169(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(None, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_170(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, None)
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_171(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_172(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, )
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_173(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output * "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_174(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "XXnativeXX")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_175(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "NATIVE")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_176(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode == 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_177(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 1:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_178(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(None)
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_179(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any(None):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_180(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output * name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_181(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size >= MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_182(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("XXstdout.logXX", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_183(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("STDOUT.LOG", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_184(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "XXstderr.logXX")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_185(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "STDERR.LOG")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_186(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError(None)
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_187(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("XXnative mutation diagnostics exceeded the bounded output budgetXX")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_188(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("NATIVE MUTATION DIAGNOSTICS EXCEEDED THE BOUNDED OUTPUT BUDGET")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_189(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = None
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_190(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["XXmutantsXX"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_191(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["MUTANTS"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_192(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(None, binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_193(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", None)
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_194(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_195(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", )
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_196(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output * "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_197(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "XXnativeXX", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_198(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "NATIVE", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_199(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["XXscopeXX"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_200(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["SCOPE"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_201(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) >= policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_202(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["XXmax_mutantsXX"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_203(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["MAX_MUTANTS"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_204(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError(None)
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_205(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("XXmutation scope exceeds the reviewed mutant budgetXX")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_206(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("MUTATION SCOPE EXCEEDS THE REVIEWED MUTANT BUDGET")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_207(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(None):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_208(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["XXstatusXX"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_209(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["STATUS"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_210(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] != "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_211(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "XXerrorXX" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_212(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "ERROR" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_213(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["XXmutantsXX"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_214(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["MUTANTS"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_215(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    None
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_216(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "XXnative mutation results include missing, untested, interrupted or errored mutantsXX"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_217(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "NATIVE MUTATION RESULTS INCLUDE MISSING, UNTESTED, INTERRUPTED OR ERRORED MUTANTS"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_218(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = None
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_219(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["XXstatusXX"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_220(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["STATUS"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_221(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "XXfailXX" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_222(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "FAIL" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_223(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(None) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_224(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["XXstatusXX"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_225(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["STATUS"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_226(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] != "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_227(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "XXsurvivedXX" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_228(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "SURVIVED" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_229(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["XXmutantsXX"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_230(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["MUTANTS"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_231(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "XXpassXX"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_232(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "PASS"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_233(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(None, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_234(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, None, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_235(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, None, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_236(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, None)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_237(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_238(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_239(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_240(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, )
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_241(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = None
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_242(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["XXstatusXX"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_243(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["STATUS"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_244(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "XXerrorXX"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_245(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "ERROR"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_246(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = None
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_247(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["XXerrorXX"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_248(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["ERROR"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_249(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(None)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_250(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = None
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_251(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["XXfinished_atXX"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_252(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["FINISHED_AT"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_253(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(None).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_254(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = None
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_255(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["XXartifactsXX"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_256(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["ARTIFACTS"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_257(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(None)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_258(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = None
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_259(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["XXpayload_digestXX"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_260(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["PAYLOAD_DIGEST"] = payload_digest(receipt)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_261(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(None)
    _write_json(output / "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_262(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(None, receipt)
    return receipt


def x_execute_mutation__mutmut_263(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", None)
    return receipt


def x_execute_mutation__mutmut_264(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(receipt)
    return receipt


def x_execute_mutation__mutmut_265(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "receipt.json", )
    return receipt


def x_execute_mutation__mutmut_266(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output * "receipt.json", receipt)
    return receipt


def x_execute_mutation__mutmut_267(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "XXreceipt.jsonXX", receipt)
    return receipt


def x_execute_mutation__mutmut_268(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    binding, files, policy = _inputs(root, base, head, broad)
    if not binding["scope"]["functions"]:
        raise MutationError(
            "no changed production function scope; no mutation receipt can be admitted", "zero-scope"
        )
    try:
        installed_version = version("mutmut")
    except PackageNotFoundError as exc:
        raise MutationError("required mutmut executable is unavailable", "dependency-unavailable") from exc
    executable = Path(sys.executable).with_name("mutmut")
    if installed_version != TOOL_VERSION or not executable.is_file():
        raise MutationError("mutation execution requires the exact pinned mutmut tool")
    if output.exists():
        raise MutationError("output already exists; retain the prior attempt and use a new directory")
    output.mkdir(parents=True)
    receipt: dict[str, Any] = {
        "schema": SCHEMA,
        **binding,
        "run_id": run_id,
        "attempt": attempt,
        "execution_id": str(uuid4()),
        "started_at": datetime.now(UTC).isoformat(),
        "status": "error",
        "mutants": [],
    }
    with TemporaryDirectory(prefix="tc-mutation-") as temporary:
        snapshot = Path(temporary)
        for path, raw in files.items():
            destination = snapshot / path
            destination.parent.mkdir(parents=True, exist_ok=True)
            destination.write_bytes(raw)
            destination.chmod(0o755 if path in binding["executable_paths"] else 0o644)
        _native_config(snapshot, files, policy)
        (output / "native-config.toml").write_bytes((snapshot / "pyproject.toml").read_bytes())
        command = [
            str(executable),
            "run",
            "--max-children",
            "2",
            *[item["selector"] for item in binding["scope"]["functions"]],
        ]
        receipt["command"] = command
        env = dict(os.environ)
        env.pop("PYTEST_ADDOPTS", None)
        env.pop("PYTEST_PLUGINS", None)
        env.pop("MUTANT_UNDER_TEST", None)
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        result = run_bounded_process(
            command,
            cwd=snapshot,
            env=env,
            timeout=policy["timeout_seconds"],
            stdout_path=output / "stdout.log",
            stderr_path=output / "stderr.log",
        )
        receipt["tool_exit_code"] = result.returncode
        native = snapshot / "mutants"
        if native.is_dir():
            shutil.copytree(native, output / "native")
        try:
            if result.returncode != 0:
                raise MutationError(f"native mutation execution did not complete: exit {result.returncode}")
            if any((output / name).stat().st_size > MAX_LOG_BYTES for name in ("stdout.log", "stderr.log")):
                raise MutationError("native mutation diagnostics exceeded the bounded output budget")
            receipt["mutants"] = _native_mutants(output / "native", binding["scope"])
            if len(receipt["mutants"]) > policy["max_mutants"]:
                raise MutationError("mutation scope exceeds the reviewed mutant budget")
            if any(item["status"] == "error" for item in receipt["mutants"]):
                raise MutationError(
                    "native mutation results include missing, untested, interrupted or errored mutants"
                )
            receipt["status"] = (
                "fail" if any(item["status"] == "survived" for item in receipt["mutants"]) else "pass"
            )
            # Re-reading the immutable base/head binding also proves the working
            # tree remains the exact candidate. Any mutation is rejected by
            # ``_inputs`` before it can return a different binding.
            _inputs(root, base, head, broad)
        except MutationError as exc:
            receipt["status"] = "error"
            receipt["error"] = str(exc)
    receipt["finished_at"] = datetime.now(UTC).isoformat()
    receipt["artifacts"] = _artifacts(output)
    receipt["payload_digest"] = payload_digest(receipt)
    _write_json(output / "RECEIPT.JSON", receipt)
    return receipt

mutants_x_execute_mutation__mutmut['_mutmut_orig'] = x_execute_mutation__mutmut_orig # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_1'] = x_execute_mutation__mutmut_1 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_2'] = x_execute_mutation__mutmut_2 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_3'] = x_execute_mutation__mutmut_3 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_4'] = x_execute_mutation__mutmut_4 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_5'] = x_execute_mutation__mutmut_5 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_6'] = x_execute_mutation__mutmut_6 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_7'] = x_execute_mutation__mutmut_7 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_8'] = x_execute_mutation__mutmut_8 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_9'] = x_execute_mutation__mutmut_9 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_10'] = x_execute_mutation__mutmut_10 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_11'] = x_execute_mutation__mutmut_11 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_12'] = x_execute_mutation__mutmut_12 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_13'] = x_execute_mutation__mutmut_13 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_14'] = x_execute_mutation__mutmut_14 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_15'] = x_execute_mutation__mutmut_15 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_16'] = x_execute_mutation__mutmut_16 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_17'] = x_execute_mutation__mutmut_17 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_18'] = x_execute_mutation__mutmut_18 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_19'] = x_execute_mutation__mutmut_19 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_20'] = x_execute_mutation__mutmut_20 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_21'] = x_execute_mutation__mutmut_21 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_22'] = x_execute_mutation__mutmut_22 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_23'] = x_execute_mutation__mutmut_23 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_24'] = x_execute_mutation__mutmut_24 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_25'] = x_execute_mutation__mutmut_25 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_26'] = x_execute_mutation__mutmut_26 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_27'] = x_execute_mutation__mutmut_27 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_28'] = x_execute_mutation__mutmut_28 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_29'] = x_execute_mutation__mutmut_29 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_30'] = x_execute_mutation__mutmut_30 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_31'] = x_execute_mutation__mutmut_31 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_32'] = x_execute_mutation__mutmut_32 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_33'] = x_execute_mutation__mutmut_33 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_34'] = x_execute_mutation__mutmut_34 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_35'] = x_execute_mutation__mutmut_35 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_36'] = x_execute_mutation__mutmut_36 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_37'] = x_execute_mutation__mutmut_37 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_38'] = x_execute_mutation__mutmut_38 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_39'] = x_execute_mutation__mutmut_39 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_40'] = x_execute_mutation__mutmut_40 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_41'] = x_execute_mutation__mutmut_41 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_42'] = x_execute_mutation__mutmut_42 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_43'] = x_execute_mutation__mutmut_43 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_44'] = x_execute_mutation__mutmut_44 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_45'] = x_execute_mutation__mutmut_45 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_46'] = x_execute_mutation__mutmut_46 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_47'] = x_execute_mutation__mutmut_47 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_48'] = x_execute_mutation__mutmut_48 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_49'] = x_execute_mutation__mutmut_49 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_50'] = x_execute_mutation__mutmut_50 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_51'] = x_execute_mutation__mutmut_51 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_52'] = x_execute_mutation__mutmut_52 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_53'] = x_execute_mutation__mutmut_53 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_54'] = x_execute_mutation__mutmut_54 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_55'] = x_execute_mutation__mutmut_55 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_56'] = x_execute_mutation__mutmut_56 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_57'] = x_execute_mutation__mutmut_57 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_58'] = x_execute_mutation__mutmut_58 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_59'] = x_execute_mutation__mutmut_59 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_60'] = x_execute_mutation__mutmut_60 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_61'] = x_execute_mutation__mutmut_61 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_62'] = x_execute_mutation__mutmut_62 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_63'] = x_execute_mutation__mutmut_63 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_64'] = x_execute_mutation__mutmut_64 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_65'] = x_execute_mutation__mutmut_65 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_66'] = x_execute_mutation__mutmut_66 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_67'] = x_execute_mutation__mutmut_67 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_68'] = x_execute_mutation__mutmut_68 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_69'] = x_execute_mutation__mutmut_69 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_70'] = x_execute_mutation__mutmut_70 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_71'] = x_execute_mutation__mutmut_71 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_72'] = x_execute_mutation__mutmut_72 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_73'] = x_execute_mutation__mutmut_73 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_74'] = x_execute_mutation__mutmut_74 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_75'] = x_execute_mutation__mutmut_75 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_76'] = x_execute_mutation__mutmut_76 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_77'] = x_execute_mutation__mutmut_77 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_78'] = x_execute_mutation__mutmut_78 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_79'] = x_execute_mutation__mutmut_79 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_80'] = x_execute_mutation__mutmut_80 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_81'] = x_execute_mutation__mutmut_81 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_82'] = x_execute_mutation__mutmut_82 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_83'] = x_execute_mutation__mutmut_83 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_84'] = x_execute_mutation__mutmut_84 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_85'] = x_execute_mutation__mutmut_85 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_86'] = x_execute_mutation__mutmut_86 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_87'] = x_execute_mutation__mutmut_87 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_88'] = x_execute_mutation__mutmut_88 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_89'] = x_execute_mutation__mutmut_89 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_90'] = x_execute_mutation__mutmut_90 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_91'] = x_execute_mutation__mutmut_91 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_92'] = x_execute_mutation__mutmut_92 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_93'] = x_execute_mutation__mutmut_93 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_94'] = x_execute_mutation__mutmut_94 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_95'] = x_execute_mutation__mutmut_95 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_96'] = x_execute_mutation__mutmut_96 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_97'] = x_execute_mutation__mutmut_97 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_98'] = x_execute_mutation__mutmut_98 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_99'] = x_execute_mutation__mutmut_99 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_100'] = x_execute_mutation__mutmut_100 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_101'] = x_execute_mutation__mutmut_101 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_102'] = x_execute_mutation__mutmut_102 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_103'] = x_execute_mutation__mutmut_103 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_104'] = x_execute_mutation__mutmut_104 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_105'] = x_execute_mutation__mutmut_105 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_106'] = x_execute_mutation__mutmut_106 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_107'] = x_execute_mutation__mutmut_107 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_108'] = x_execute_mutation__mutmut_108 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_109'] = x_execute_mutation__mutmut_109 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_110'] = x_execute_mutation__mutmut_110 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_111'] = x_execute_mutation__mutmut_111 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_112'] = x_execute_mutation__mutmut_112 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_113'] = x_execute_mutation__mutmut_113 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_114'] = x_execute_mutation__mutmut_114 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_115'] = x_execute_mutation__mutmut_115 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_116'] = x_execute_mutation__mutmut_116 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_117'] = x_execute_mutation__mutmut_117 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_118'] = x_execute_mutation__mutmut_118 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_119'] = x_execute_mutation__mutmut_119 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_120'] = x_execute_mutation__mutmut_120 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_121'] = x_execute_mutation__mutmut_121 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_122'] = x_execute_mutation__mutmut_122 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_123'] = x_execute_mutation__mutmut_123 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_124'] = x_execute_mutation__mutmut_124 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_125'] = x_execute_mutation__mutmut_125 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_126'] = x_execute_mutation__mutmut_126 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_127'] = x_execute_mutation__mutmut_127 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_128'] = x_execute_mutation__mutmut_128 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_129'] = x_execute_mutation__mutmut_129 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_130'] = x_execute_mutation__mutmut_130 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_131'] = x_execute_mutation__mutmut_131 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_132'] = x_execute_mutation__mutmut_132 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_133'] = x_execute_mutation__mutmut_133 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_134'] = x_execute_mutation__mutmut_134 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_135'] = x_execute_mutation__mutmut_135 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_136'] = x_execute_mutation__mutmut_136 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_137'] = x_execute_mutation__mutmut_137 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_138'] = x_execute_mutation__mutmut_138 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_139'] = x_execute_mutation__mutmut_139 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_140'] = x_execute_mutation__mutmut_140 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_141'] = x_execute_mutation__mutmut_141 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_142'] = x_execute_mutation__mutmut_142 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_143'] = x_execute_mutation__mutmut_143 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_144'] = x_execute_mutation__mutmut_144 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_145'] = x_execute_mutation__mutmut_145 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_146'] = x_execute_mutation__mutmut_146 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_147'] = x_execute_mutation__mutmut_147 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_148'] = x_execute_mutation__mutmut_148 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_149'] = x_execute_mutation__mutmut_149 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_150'] = x_execute_mutation__mutmut_150 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_151'] = x_execute_mutation__mutmut_151 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_152'] = x_execute_mutation__mutmut_152 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_153'] = x_execute_mutation__mutmut_153 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_154'] = x_execute_mutation__mutmut_154 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_155'] = x_execute_mutation__mutmut_155 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_156'] = x_execute_mutation__mutmut_156 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_157'] = x_execute_mutation__mutmut_157 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_158'] = x_execute_mutation__mutmut_158 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_159'] = x_execute_mutation__mutmut_159 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_160'] = x_execute_mutation__mutmut_160 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_161'] = x_execute_mutation__mutmut_161 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_162'] = x_execute_mutation__mutmut_162 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_163'] = x_execute_mutation__mutmut_163 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_164'] = x_execute_mutation__mutmut_164 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_165'] = x_execute_mutation__mutmut_165 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_166'] = x_execute_mutation__mutmut_166 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_167'] = x_execute_mutation__mutmut_167 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_168'] = x_execute_mutation__mutmut_168 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_169'] = x_execute_mutation__mutmut_169 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_170'] = x_execute_mutation__mutmut_170 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_171'] = x_execute_mutation__mutmut_171 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_172'] = x_execute_mutation__mutmut_172 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_173'] = x_execute_mutation__mutmut_173 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_174'] = x_execute_mutation__mutmut_174 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_175'] = x_execute_mutation__mutmut_175 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_176'] = x_execute_mutation__mutmut_176 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_177'] = x_execute_mutation__mutmut_177 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_178'] = x_execute_mutation__mutmut_178 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_179'] = x_execute_mutation__mutmut_179 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_180'] = x_execute_mutation__mutmut_180 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_181'] = x_execute_mutation__mutmut_181 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_182'] = x_execute_mutation__mutmut_182 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_183'] = x_execute_mutation__mutmut_183 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_184'] = x_execute_mutation__mutmut_184 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_185'] = x_execute_mutation__mutmut_185 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_186'] = x_execute_mutation__mutmut_186 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_187'] = x_execute_mutation__mutmut_187 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_188'] = x_execute_mutation__mutmut_188 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_189'] = x_execute_mutation__mutmut_189 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_190'] = x_execute_mutation__mutmut_190 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_191'] = x_execute_mutation__mutmut_191 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_192'] = x_execute_mutation__mutmut_192 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_193'] = x_execute_mutation__mutmut_193 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_194'] = x_execute_mutation__mutmut_194 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_195'] = x_execute_mutation__mutmut_195 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_196'] = x_execute_mutation__mutmut_196 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_197'] = x_execute_mutation__mutmut_197 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_198'] = x_execute_mutation__mutmut_198 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_199'] = x_execute_mutation__mutmut_199 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_200'] = x_execute_mutation__mutmut_200 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_201'] = x_execute_mutation__mutmut_201 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_202'] = x_execute_mutation__mutmut_202 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_203'] = x_execute_mutation__mutmut_203 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_204'] = x_execute_mutation__mutmut_204 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_205'] = x_execute_mutation__mutmut_205 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_206'] = x_execute_mutation__mutmut_206 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_207'] = x_execute_mutation__mutmut_207 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_208'] = x_execute_mutation__mutmut_208 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_209'] = x_execute_mutation__mutmut_209 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_210'] = x_execute_mutation__mutmut_210 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_211'] = x_execute_mutation__mutmut_211 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_212'] = x_execute_mutation__mutmut_212 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_213'] = x_execute_mutation__mutmut_213 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_214'] = x_execute_mutation__mutmut_214 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_215'] = x_execute_mutation__mutmut_215 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_216'] = x_execute_mutation__mutmut_216 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_217'] = x_execute_mutation__mutmut_217 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_218'] = x_execute_mutation__mutmut_218 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_219'] = x_execute_mutation__mutmut_219 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_220'] = x_execute_mutation__mutmut_220 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_221'] = x_execute_mutation__mutmut_221 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_222'] = x_execute_mutation__mutmut_222 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_223'] = x_execute_mutation__mutmut_223 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_224'] = x_execute_mutation__mutmut_224 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_225'] = x_execute_mutation__mutmut_225 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_226'] = x_execute_mutation__mutmut_226 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_227'] = x_execute_mutation__mutmut_227 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_228'] = x_execute_mutation__mutmut_228 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_229'] = x_execute_mutation__mutmut_229 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_230'] = x_execute_mutation__mutmut_230 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_231'] = x_execute_mutation__mutmut_231 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_232'] = x_execute_mutation__mutmut_232 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_233'] = x_execute_mutation__mutmut_233 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_234'] = x_execute_mutation__mutmut_234 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_235'] = x_execute_mutation__mutmut_235 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_236'] = x_execute_mutation__mutmut_236 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_237'] = x_execute_mutation__mutmut_237 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_238'] = x_execute_mutation__mutmut_238 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_239'] = x_execute_mutation__mutmut_239 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_240'] = x_execute_mutation__mutmut_240 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_241'] = x_execute_mutation__mutmut_241 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_242'] = x_execute_mutation__mutmut_242 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_243'] = x_execute_mutation__mutmut_243 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_244'] = x_execute_mutation__mutmut_244 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_245'] = x_execute_mutation__mutmut_245 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_246'] = x_execute_mutation__mutmut_246 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_247'] = x_execute_mutation__mutmut_247 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_248'] = x_execute_mutation__mutmut_248 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_249'] = x_execute_mutation__mutmut_249 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_250'] = x_execute_mutation__mutmut_250 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_251'] = x_execute_mutation__mutmut_251 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_252'] = x_execute_mutation__mutmut_252 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_253'] = x_execute_mutation__mutmut_253 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_254'] = x_execute_mutation__mutmut_254 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_255'] = x_execute_mutation__mutmut_255 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_256'] = x_execute_mutation__mutmut_256 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_257'] = x_execute_mutation__mutmut_257 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_258'] = x_execute_mutation__mutmut_258 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_259'] = x_execute_mutation__mutmut_259 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_260'] = x_execute_mutation__mutmut_260 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_261'] = x_execute_mutation__mutmut_261 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_262'] = x_execute_mutation__mutmut_262 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_263'] = x_execute_mutation__mutmut_263 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_264'] = x_execute_mutation__mutmut_264 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_265'] = x_execute_mutation__mutmut_265 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_266'] = x_execute_mutation__mutmut_266 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_267'] = x_execute_mutation__mutmut_267 # type: ignore # mutmut generated
mutants_x_execute_mutation__mutmut['x_execute_mutation__mutmut_268'] = x_execute_mutation__mutmut_268 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__validate_execution_timestamps__mutmut)
def _validate_execution_timestamps(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_orig(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_1(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = None
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_2(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(None)
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_3(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["XXstarted_atXX"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_4(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["STARTED_AT"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_5(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = None
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_6(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(None)
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_7(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["XXfinished_atXX"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_8(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["FINISHED_AT"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_9(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now and (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_10(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_11(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started < finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_12(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished < now or (now - finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_13(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now + finished).total_seconds() > 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_14(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() >= 86400:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_15(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86401:
        raise MutationError("stale or reversed mutation execution timestamps")


def x__validate_execution_timestamps__mutmut_16(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError(None)


def x__validate_execution_timestamps__mutmut_17(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("XXstale or reversed mutation execution timestampsXX")


def x__validate_execution_timestamps__mutmut_18(receipt: dict[str, Any], now: datetime) -> None:
    """Check the closed freshness interval against the admission clock reading."""
    started = datetime.fromisoformat(receipt["started_at"])
    finished = datetime.fromisoformat(receipt["finished_at"])
    if not started <= finished <= now or (now - finished).total_seconds() > 86400:
        raise MutationError("STALE OR REVERSED MUTATION EXECUTION TIMESTAMPS")

mutants_x__validate_execution_timestamps__mutmut['_mutmut_orig'] = x__validate_execution_timestamps__mutmut_orig # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_1'] = x__validate_execution_timestamps__mutmut_1 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_2'] = x__validate_execution_timestamps__mutmut_2 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_3'] = x__validate_execution_timestamps__mutmut_3 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_4'] = x__validate_execution_timestamps__mutmut_4 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_5'] = x__validate_execution_timestamps__mutmut_5 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_6'] = x__validate_execution_timestamps__mutmut_6 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_7'] = x__validate_execution_timestamps__mutmut_7 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_8'] = x__validate_execution_timestamps__mutmut_8 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_9'] = x__validate_execution_timestamps__mutmut_9 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_10'] = x__validate_execution_timestamps__mutmut_10 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_11'] = x__validate_execution_timestamps__mutmut_11 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_12'] = x__validate_execution_timestamps__mutmut_12 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_13'] = x__validate_execution_timestamps__mutmut_13 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_14'] = x__validate_execution_timestamps__mutmut_14 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_15'] = x__validate_execution_timestamps__mutmut_15 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_16'] = x__validate_execution_timestamps__mutmut_16 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_17'] = x__validate_execution_timestamps__mutmut_17 # type: ignore # mutmut generated
mutants_x__validate_execution_timestamps__mutmut['x__validate_execution_timestamps__mutmut_18'] = x__validate_execution_timestamps__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_validate_mutation_receipt__mutmut)
def validate_mutation_receipt(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_orig(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_1(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = True
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_2(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_3(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output * "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_4(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "XXreceipt.jsonXX").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_5(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "RECEIPT.JSON").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_6(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError(None, "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_7(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", None)
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_8(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_9(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", )
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_10(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("XXmissing mutation receiptXX", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_11(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("MISSING MUTATION RECEIPT", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_12(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "XXmissing-receiptXX")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_13(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "MISSING-RECEIPT")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_14(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = None
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_15(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(None)
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_16(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output * "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_17(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "XXreceipt.jsonXX")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_18(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "RECEIPT.JSON")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_19(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = None
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_20(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop(None, None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_21(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop(None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_22(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", )
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_23(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("XXpayload_digestXX", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_24(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("PAYLOAD_DIGEST", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_25(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest == payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_26(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(None):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_27(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError(None)
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_28(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("XXmutation receipt digest mismatchXX")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_29(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("MUTATION RECEIPT DIGEST MISMATCH")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_30(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = None
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_31(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(None, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_32(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, None, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_33(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, None, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_34(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, None)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_35(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_36(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_37(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_38(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, )
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_39(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"XXschemaXX": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_40(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"SCHEMA": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_41(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "XXrun_idXX": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_42(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "RUN_ID": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_43(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "XXattemptXX": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_44(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "ATTEMPT": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_45(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected and type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_46(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(None) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_47(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) == expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_48(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(None) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_49(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(None)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_50(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_51(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(None):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_52(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(None)
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_53(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(None)
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_54(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["XXexecution_idXX"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_55(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["EXECUTION_ID"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_56(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(None, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_57(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, None)
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_58(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_59(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, )
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_60(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(None))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_61(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError(None) from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_62(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("XXinvalid mutation execution identity or timestampsXX") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_63(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("INVALID MUTATION EXECUTION IDENTITY OR TIMESTAMPS") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_64(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = None
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_65(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(None)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_66(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts and receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_67(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_68(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get(None) != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_69(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("XXartifactsXX") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_70(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("ARTIFACTS") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_71(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") == artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_72(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError(None)
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_73(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("XXmissing, extra or altered native mutation artifactsXX")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_74(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("MISSING, EXTRA OR ALTERED NATIVE MUTATION ARTIFACTS")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_75(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = None
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_76(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"XXnative-config.tomlXX", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_77(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"NATIVE-CONFIG.TOML", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_78(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "XXstdout.logXX", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_79(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "STDOUT.LOG", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_80(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "XXstderr.logXX", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_81(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "STDERR.LOG", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_82(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "XXnative/mutmut-stats.jsonXX"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_83(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "NATIVE/MUTMUT-STATS.JSON"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_84(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_85(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required < {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_86(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["XXpathXX"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_87(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["PATH"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_88(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError(None)
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_89(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("XXmissing required native mutation outputsXX")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_90(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("MISSING REQUIRED NATIVE MUTATION OUTPUTS")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_91(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = None
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_92(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(None, binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_93(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", None)
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_94(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_95(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", )
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_96(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output * "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_97(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "XXnativeXX", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_98(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "NATIVE", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_99(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["XXscopeXX"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_100(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["SCOPE"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_101(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual and len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_102(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get(None) != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_103(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("XXmutantsXX") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_104(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("MUTANTS") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_105(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") == actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_106(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) >= policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_107(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["XXmax_mutantsXX"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_108(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["MAX_MUTANTS"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_109(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError(None)
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_110(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("XXnative mutant set does not match the claimed evidenceXX")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_111(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("NATIVE MUTANT SET DOES NOT MATCH THE CLAIMED EVIDENCE")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_112(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0 and any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_113(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass" and receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_114(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get(None) != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_115(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("XXstatusXX") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_116(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("STATUS") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_117(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") == "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_118(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "XXpassXX"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_119(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "PASS"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_120(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get(None) != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_121(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("XXtool_exit_codeXX") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_122(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("TOOL_EXIT_CODE") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_123(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") == 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_124(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 1
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_125(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(None)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_126(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["XXstatusXX"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_127(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["STATUS"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_128(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] == "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_129(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "XXkilledXX" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_130(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "KILLED" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_131(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError(None)
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_132(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("XXmutation admission requires terminal killed results for every selected mutantXX")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_133(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("MUTATION ADMISSION REQUIRES TERMINAL KILLED RESULTS FOR EVERY SELECTED MUTANT")
    receipt["payload_digest"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_134(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["payload_digest"] = None
    return receipt


def x_validate_mutation_receipt__mutmut_135(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["XXpayload_digestXX"] = recorded_digest
    return receipt


def x_validate_mutation_receipt__mutmut_136(
    root: Path, base: str, head: str, output: Path, *, run_id: str, attempt: int, broad: bool = False
) -> dict[str, Any]:
    """Admit only complete current native results; outer claims are insufficient."""
    if not (output / "receipt.json").is_file():
        raise MutationError("missing mutation receipt", "missing-receipt")
    receipt = _json(output / "receipt.json")
    recorded_digest = receipt.pop("payload_digest", None)
    if recorded_digest != payload_digest(receipt):
        raise MutationError("mutation receipt digest mismatch")
    binding, _, policy = _inputs(root, base, head, broad)
    for key, expected in {"schema": SCHEMA, **binding, "run_id": run_id, "attempt": attempt}.items():
        if receipt.get(key) != expected or type(receipt.get(key)) is not type(expected):
            raise MutationError(f"mutation receipt {key} mismatch")
    try:
        UUID(receipt["execution_id"])
        _validate_execution_timestamps(receipt, datetime.now(UTC))
    except (KeyError, TypeError, ValueError) as exc:
        raise MutationError("invalid mutation execution identity or timestamps") from exc
    artifacts = _artifacts(output)
    if not artifacts or receipt.get("artifacts") != artifacts:
        raise MutationError("missing, extra or altered native mutation artifacts")
    required = {"native-config.toml", "stdout.log", "stderr.log", "native/mutmut-stats.json"}
    if not required <= {artifact["path"] for artifact in artifacts}:
        raise MutationError("missing required native mutation outputs")
    actual = _native_mutants(output / "native", binding["scope"])
    if receipt.get("mutants") != actual or len(actual) > policy["max_mutants"]:
        raise MutationError("native mutant set does not match the claimed evidence")
    if (
        receipt.get("status") != "pass"
        or receipt.get("tool_exit_code") != 0
        or any(item["status"] != "killed" for item in actual)
    ):
        raise MutationError("mutation admission requires terminal killed results for every selected mutant")
    receipt["PAYLOAD_DIGEST"] = recorded_digest
    return receipt

mutants_x_validate_mutation_receipt__mutmut['_mutmut_orig'] = x_validate_mutation_receipt__mutmut_orig # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_1'] = x_validate_mutation_receipt__mutmut_1 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_2'] = x_validate_mutation_receipt__mutmut_2 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_3'] = x_validate_mutation_receipt__mutmut_3 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_4'] = x_validate_mutation_receipt__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_5'] = x_validate_mutation_receipt__mutmut_5 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_6'] = x_validate_mutation_receipt__mutmut_6 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_7'] = x_validate_mutation_receipt__mutmut_7 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_8'] = x_validate_mutation_receipt__mutmut_8 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_9'] = x_validate_mutation_receipt__mutmut_9 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_10'] = x_validate_mutation_receipt__mutmut_10 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_11'] = x_validate_mutation_receipt__mutmut_11 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_12'] = x_validate_mutation_receipt__mutmut_12 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_13'] = x_validate_mutation_receipt__mutmut_13 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_14'] = x_validate_mutation_receipt__mutmut_14 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_15'] = x_validate_mutation_receipt__mutmut_15 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_16'] = x_validate_mutation_receipt__mutmut_16 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_17'] = x_validate_mutation_receipt__mutmut_17 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_18'] = x_validate_mutation_receipt__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_19'] = x_validate_mutation_receipt__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_20'] = x_validate_mutation_receipt__mutmut_20 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_21'] = x_validate_mutation_receipt__mutmut_21 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_22'] = x_validate_mutation_receipt__mutmut_22 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_23'] = x_validate_mutation_receipt__mutmut_23 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_24'] = x_validate_mutation_receipt__mutmut_24 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_25'] = x_validate_mutation_receipt__mutmut_25 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_26'] = x_validate_mutation_receipt__mutmut_26 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_27'] = x_validate_mutation_receipt__mutmut_27 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_28'] = x_validate_mutation_receipt__mutmut_28 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_29'] = x_validate_mutation_receipt__mutmut_29 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_30'] = x_validate_mutation_receipt__mutmut_30 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_31'] = x_validate_mutation_receipt__mutmut_31 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_32'] = x_validate_mutation_receipt__mutmut_32 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_33'] = x_validate_mutation_receipt__mutmut_33 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_34'] = x_validate_mutation_receipt__mutmut_34 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_35'] = x_validate_mutation_receipt__mutmut_35 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_36'] = x_validate_mutation_receipt__mutmut_36 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_37'] = x_validate_mutation_receipt__mutmut_37 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_38'] = x_validate_mutation_receipt__mutmut_38 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_39'] = x_validate_mutation_receipt__mutmut_39 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_40'] = x_validate_mutation_receipt__mutmut_40 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_41'] = x_validate_mutation_receipt__mutmut_41 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_42'] = x_validate_mutation_receipt__mutmut_42 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_43'] = x_validate_mutation_receipt__mutmut_43 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_44'] = x_validate_mutation_receipt__mutmut_44 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_45'] = x_validate_mutation_receipt__mutmut_45 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_46'] = x_validate_mutation_receipt__mutmut_46 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_47'] = x_validate_mutation_receipt__mutmut_47 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_48'] = x_validate_mutation_receipt__mutmut_48 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_49'] = x_validate_mutation_receipt__mutmut_49 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_50'] = x_validate_mutation_receipt__mutmut_50 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_51'] = x_validate_mutation_receipt__mutmut_51 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_52'] = x_validate_mutation_receipt__mutmut_52 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_53'] = x_validate_mutation_receipt__mutmut_53 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_54'] = x_validate_mutation_receipt__mutmut_54 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_55'] = x_validate_mutation_receipt__mutmut_55 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_56'] = x_validate_mutation_receipt__mutmut_56 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_57'] = x_validate_mutation_receipt__mutmut_57 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_58'] = x_validate_mutation_receipt__mutmut_58 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_59'] = x_validate_mutation_receipt__mutmut_59 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_60'] = x_validate_mutation_receipt__mutmut_60 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_61'] = x_validate_mutation_receipt__mutmut_61 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_62'] = x_validate_mutation_receipt__mutmut_62 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_63'] = x_validate_mutation_receipt__mutmut_63 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_64'] = x_validate_mutation_receipt__mutmut_64 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_65'] = x_validate_mutation_receipt__mutmut_65 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_66'] = x_validate_mutation_receipt__mutmut_66 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_67'] = x_validate_mutation_receipt__mutmut_67 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_68'] = x_validate_mutation_receipt__mutmut_68 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_69'] = x_validate_mutation_receipt__mutmut_69 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_70'] = x_validate_mutation_receipt__mutmut_70 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_71'] = x_validate_mutation_receipt__mutmut_71 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_72'] = x_validate_mutation_receipt__mutmut_72 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_73'] = x_validate_mutation_receipt__mutmut_73 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_74'] = x_validate_mutation_receipt__mutmut_74 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_75'] = x_validate_mutation_receipt__mutmut_75 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_76'] = x_validate_mutation_receipt__mutmut_76 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_77'] = x_validate_mutation_receipt__mutmut_77 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_78'] = x_validate_mutation_receipt__mutmut_78 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_79'] = x_validate_mutation_receipt__mutmut_79 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_80'] = x_validate_mutation_receipt__mutmut_80 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_81'] = x_validate_mutation_receipt__mutmut_81 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_82'] = x_validate_mutation_receipt__mutmut_82 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_83'] = x_validate_mutation_receipt__mutmut_83 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_84'] = x_validate_mutation_receipt__mutmut_84 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_85'] = x_validate_mutation_receipt__mutmut_85 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_86'] = x_validate_mutation_receipt__mutmut_86 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_87'] = x_validate_mutation_receipt__mutmut_87 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_88'] = x_validate_mutation_receipt__mutmut_88 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_89'] = x_validate_mutation_receipt__mutmut_89 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_90'] = x_validate_mutation_receipt__mutmut_90 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_91'] = x_validate_mutation_receipt__mutmut_91 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_92'] = x_validate_mutation_receipt__mutmut_92 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_93'] = x_validate_mutation_receipt__mutmut_93 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_94'] = x_validate_mutation_receipt__mutmut_94 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_95'] = x_validate_mutation_receipt__mutmut_95 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_96'] = x_validate_mutation_receipt__mutmut_96 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_97'] = x_validate_mutation_receipt__mutmut_97 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_98'] = x_validate_mutation_receipt__mutmut_98 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_99'] = x_validate_mutation_receipt__mutmut_99 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_100'] = x_validate_mutation_receipt__mutmut_100 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_101'] = x_validate_mutation_receipt__mutmut_101 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_102'] = x_validate_mutation_receipt__mutmut_102 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_103'] = x_validate_mutation_receipt__mutmut_103 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_104'] = x_validate_mutation_receipt__mutmut_104 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_105'] = x_validate_mutation_receipt__mutmut_105 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_106'] = x_validate_mutation_receipt__mutmut_106 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_107'] = x_validate_mutation_receipt__mutmut_107 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_108'] = x_validate_mutation_receipt__mutmut_108 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_109'] = x_validate_mutation_receipt__mutmut_109 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_110'] = x_validate_mutation_receipt__mutmut_110 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_111'] = x_validate_mutation_receipt__mutmut_111 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_112'] = x_validate_mutation_receipt__mutmut_112 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_113'] = x_validate_mutation_receipt__mutmut_113 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_114'] = x_validate_mutation_receipt__mutmut_114 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_115'] = x_validate_mutation_receipt__mutmut_115 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_116'] = x_validate_mutation_receipt__mutmut_116 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_117'] = x_validate_mutation_receipt__mutmut_117 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_118'] = x_validate_mutation_receipt__mutmut_118 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_119'] = x_validate_mutation_receipt__mutmut_119 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_120'] = x_validate_mutation_receipt__mutmut_120 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_121'] = x_validate_mutation_receipt__mutmut_121 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_122'] = x_validate_mutation_receipt__mutmut_122 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_123'] = x_validate_mutation_receipt__mutmut_123 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_124'] = x_validate_mutation_receipt__mutmut_124 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_125'] = x_validate_mutation_receipt__mutmut_125 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_126'] = x_validate_mutation_receipt__mutmut_126 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_127'] = x_validate_mutation_receipt__mutmut_127 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_128'] = x_validate_mutation_receipt__mutmut_128 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_129'] = x_validate_mutation_receipt__mutmut_129 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_130'] = x_validate_mutation_receipt__mutmut_130 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_131'] = x_validate_mutation_receipt__mutmut_131 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_132'] = x_validate_mutation_receipt__mutmut_132 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_133'] = x_validate_mutation_receipt__mutmut_133 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_134'] = x_validate_mutation_receipt__mutmut_134 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_135'] = x_validate_mutation_receipt__mutmut_135 # type: ignore # mutmut generated
mutants_x_validate_mutation_receipt__mutmut['x_validate_mutation_receipt__mutmut_136'] = x_validate_mutation_receipt__mutmut_136 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_orig(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_1(argv: list[str] | None = None) -> int:
    parser = None
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_2(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog=None)
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_3(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="XXtc-fitness mutationXX")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_4(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="TC-FITNESS MUTATION")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_5(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument(None, choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_6(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=None)
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_7(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument(choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_8(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", )
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_9(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("XXactionXX", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_10(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("ACTION", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_11(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("XXplanXX", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_12(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("PLAN", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_13(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "XXcheckXX", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_14(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "CHECK", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_15(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "XXrunXX", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_16(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "RUN", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_17(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "XXverifyXX"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_18(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "VERIFY"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_19(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument(None, type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_20(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=None, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_21(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=None)
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_22(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument(type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_23(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_24(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, )
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_25(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("XX--repo-rootXX", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_26(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--REPO-ROOT", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_27(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument(None, required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_28(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=None)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_29(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument(required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_30(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", )
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_31(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("XX--baseXX", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_32(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--BASE", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_33(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=False)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_34(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_35(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=None)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_36(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument(required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_37(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", )
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_38(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("XX--headXX", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_39(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--HEAD", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_40(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=False)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_41(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument(None, type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_42(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=None, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_43(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=None)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_44(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument(type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_45(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_46(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, )
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_47(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("XX--outputXX", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_48(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--OUTPUT", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_49(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=False)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_50(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument(None, required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_51(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=None)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_52(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument(required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_53(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", )
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_54(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("XX--run-idXX", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_55(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--RUN-ID", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_56(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=False)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_57(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument(None, type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_58(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=None, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_59(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=None)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_60(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument(type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_61(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_62(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, )
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_63(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("XX--attemptXX", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_64(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--ATTEMPT", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_65(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=False)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_66(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument(None, action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_67(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action=None)
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_68(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument(action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_69(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", )
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_70(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("XX--broadXX", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_71(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--BROAD", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_72(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="XXstore_trueXX")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_73(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="STORE_TRUE")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_74(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = None
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_75(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(None)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_76(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() and args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_77(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_78(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt <= 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_79(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 2:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_80(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError(None)
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_81(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("XXrun-id and positive attempt are requiredXX")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_82(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("RUN-ID AND POSITIVE ATTEMPT ARE REQUIRED")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_83(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = None
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_84(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action not in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_85(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"XXplanXX", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_86(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"PLAN", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_87(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "XXcheckXX"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_88(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "CHECK"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_89(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = None
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_90(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(None, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_91(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, None, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_92(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, None, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_93(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, None)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_94(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_95(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_96(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_97(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, )
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_98(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = None
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_99(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "XXschemaXX": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_100(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "SCHEMA": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_101(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "XXtc.fitness/mutation-plan/v1XX",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_102(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "TC.FITNESS/MUTATION-PLAN/V1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_103(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "XXrequiredXX": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_104(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "REQUIRED": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_105(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["XXscopeXX"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_106(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["SCOPE"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_107(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["XXrequiredXX"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_108(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["REQUIRED"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_109(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action != "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_110(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "XXplanXX":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_111(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "PLAN":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_112(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(None, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_113(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, None)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_114(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_115(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, )
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_116(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(None)
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_117(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(None))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_118(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 1
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_119(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=None, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_120(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=None)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_121(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_122(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, )
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_123(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=False, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_124(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=True)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_125(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(None, value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_126(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", None)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_127(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_128(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", )
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_129(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output * "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_130(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "XXselection.jsonXX", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_131(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "SELECTION.JSON", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_132(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_133(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["XXrequiredXX"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_134(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["REQUIRED"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_135(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(None)
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_136(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps(None))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_137(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"XXstatusXX": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_138(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"STATUS": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_139(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "XXnot-requiredXX", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_140(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "NOT-REQUIRED", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_141(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "XXselectionXX": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_142(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "SELECTION": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_143(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(None)}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_144(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output * "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_145(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "XXselection.jsonXX")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_146(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "SELECTION.JSON")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_147(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 1
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_148(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = None
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_149(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output * "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_150(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "XXrunXX"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_151(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "RUN"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_152(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = None
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_153(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action not in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_154(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"XXrunXX", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_155(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"RUN", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_156(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "XXcheckXX"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_157(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "CHECK"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_158(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = None
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_159(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            None, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_160(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, None, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_161(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, None, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_162(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, None, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_163(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=None, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_164(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=None, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_165(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=None
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_166(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_167(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_168(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_169(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_170(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_171(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_172(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_173(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} or receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_174(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action not in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_175(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"XXrunXX", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_176(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"RUN", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_177(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "XXcheckXX"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_178(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "CHECK"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_179(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["XXstatusXX"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_180(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["STATUS"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_181(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] != "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_182(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "XXpassXX":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_183(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "PASS":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_184(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = None
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_185(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                None, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_186(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, None, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_187(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, None, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_188(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, None, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_189(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=None, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_190(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=None, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_191(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=None
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_192(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_193(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_194(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_195(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_196(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_197(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_198(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_199(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(None)
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_200(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps(None))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_201(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"XXstatusXX": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_202(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"STATUS": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_203(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["XXstatusXX"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_204(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["STATUS"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_205(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "XXreceiptXX": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_206(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "RECEIPT": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_207(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(None)}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_208(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output * "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_209(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "XXreceipt.jsonXX")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_210(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "RECEIPT.JSON")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_211(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"XXpassXX": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_212(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"PASS": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_213(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 1, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_214(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "XXfailXX": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_215(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "FAIL": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_216(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 2, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_217(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "XXerrorXX": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_218(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "ERROR": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_219(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 3}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_220(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["XXstatusXX"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_221(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["STATUS"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_222(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            None
        )
        return 2


def x_main__mutmut_223(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                None
            )
        )
        return 2


def x_main__mutmut_224(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"XXstatusXX": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_225(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"STATUS": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_226(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "XXerrorXX", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_227(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "ERROR", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_228(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "XXcodeXX": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_229(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "CODE": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_230(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(None, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_231(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, None, "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_232(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", None), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_233(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr("code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_234(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_235(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", ), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_236(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "XXcodeXX", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_237(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "CODE", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_238(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "XXinvalid-evidenceXX"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_239(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "INVALID-EVIDENCE"), "message": str(exc)}
            )
        )
        return 2


def x_main__mutmut_240(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "XXmessageXX": str(exc)}
            )
        )
        return 2


def x_main__mutmut_241(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "MESSAGE": str(exc)}
            )
        )
        return 2


def x_main__mutmut_242(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(None)}
            )
        )
        return 2


def x_main__mutmut_243(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(prog="tc-fitness mutation")
    parser.add_argument("action", choices=("plan", "check", "run", "verify"))
    parser.add_argument("--repo-root", type=Path, default=Path.cwd())
    parser.add_argument("--base", required=True)
    parser.add_argument("--head", required=True)
    parser.add_argument("--output", type=Path, required=True)
    parser.add_argument("--run-id", required=True)
    parser.add_argument("--attempt", type=int, required=True)
    parser.add_argument("--broad", action="store_true")
    args = parser.parse_args(argv)
    try:
        if not args.run_id.strip() or args.attempt < 1:
            raise MutationError("run-id and positive attempt are required")
        root, output = args.repo_root.resolve(), args.output.resolve()
        if args.action in {"plan", "check"}:
            binding, _, _ = _inputs(root, args.base, args.head, args.broad)
            value = {
                "schema": "tc.fitness/mutation-plan/v1",
                **binding,
                "required": binding["scope"]["required"],
            }
            if args.action == "plan":
                _write_json(output, value)
                print(json.dumps(value))
                return 0
            output.mkdir(parents=True, exist_ok=False)
            _write_json(output / "selection.json", value)
            if not value["required"]:
                print(json.dumps({"status": "not-required", "selection": str(output / "selection.json")}))
                return 0
            output = output / "run"
        function = execute_mutation if args.action in {"run", "check"} else validate_mutation_receipt
        receipt = function(
            root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
        )
        if args.action in {"run", "check"} and receipt["status"] == "pass":
            receipt = validate_mutation_receipt(
                root, args.base, args.head, output, run_id=args.run_id, attempt=args.attempt, broad=args.broad
            )
        print(json.dumps({"status": receipt["status"], "receipt": str(output / "receipt.json")}))
        return {"pass": 0, "fail": 1, "error": 2}[receipt["status"]]
    except (MutationError, OSError, KeyError, TypeError, ValueError) as exc:
        print(
            json.dumps(
                {"status": "error", "code": getattr(exc, "code", "invalid-evidence"), "message": str(exc)}
            )
        )
        return 3

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_2'] = x_main__mutmut_2 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_3'] = x_main__mutmut_3 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_4'] = x_main__mutmut_4 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_5'] = x_main__mutmut_5 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_6'] = x_main__mutmut_6 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_7'] = x_main__mutmut_7 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_8'] = x_main__mutmut_8 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_9'] = x_main__mutmut_9 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_10'] = x_main__mutmut_10 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_11'] = x_main__mutmut_11 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_12'] = x_main__mutmut_12 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_13'] = x_main__mutmut_13 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_14'] = x_main__mutmut_14 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_15'] = x_main__mutmut_15 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_16'] = x_main__mutmut_16 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_17'] = x_main__mutmut_17 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_18'] = x_main__mutmut_18 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_19'] = x_main__mutmut_19 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_20'] = x_main__mutmut_20 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_21'] = x_main__mutmut_21 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_22'] = x_main__mutmut_22 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_23'] = x_main__mutmut_23 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_24'] = x_main__mutmut_24 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_25'] = x_main__mutmut_25 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_26'] = x_main__mutmut_26 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_27'] = x_main__mutmut_27 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_28'] = x_main__mutmut_28 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_29'] = x_main__mutmut_29 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_30'] = x_main__mutmut_30 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_31'] = x_main__mutmut_31 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_32'] = x_main__mutmut_32 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_33'] = x_main__mutmut_33 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_34'] = x_main__mutmut_34 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_35'] = x_main__mutmut_35 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_36'] = x_main__mutmut_36 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_37'] = x_main__mutmut_37 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_38'] = x_main__mutmut_38 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_39'] = x_main__mutmut_39 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_40'] = x_main__mutmut_40 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_41'] = x_main__mutmut_41 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_42'] = x_main__mutmut_42 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_43'] = x_main__mutmut_43 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_44'] = x_main__mutmut_44 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_45'] = x_main__mutmut_45 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_46'] = x_main__mutmut_46 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_47'] = x_main__mutmut_47 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_48'] = x_main__mutmut_48 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_49'] = x_main__mutmut_49 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_50'] = x_main__mutmut_50 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_51'] = x_main__mutmut_51 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_52'] = x_main__mutmut_52 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_53'] = x_main__mutmut_53 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_54'] = x_main__mutmut_54 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_55'] = x_main__mutmut_55 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_56'] = x_main__mutmut_56 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_57'] = x_main__mutmut_57 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_58'] = x_main__mutmut_58 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_59'] = x_main__mutmut_59 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_60'] = x_main__mutmut_60 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_61'] = x_main__mutmut_61 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_62'] = x_main__mutmut_62 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_63'] = x_main__mutmut_63 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_64'] = x_main__mutmut_64 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_65'] = x_main__mutmut_65 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_66'] = x_main__mutmut_66 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_67'] = x_main__mutmut_67 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_68'] = x_main__mutmut_68 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_69'] = x_main__mutmut_69 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_70'] = x_main__mutmut_70 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_71'] = x_main__mutmut_71 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_72'] = x_main__mutmut_72 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_73'] = x_main__mutmut_73 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_74'] = x_main__mutmut_74 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_75'] = x_main__mutmut_75 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_76'] = x_main__mutmut_76 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_77'] = x_main__mutmut_77 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_78'] = x_main__mutmut_78 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_79'] = x_main__mutmut_79 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_80'] = x_main__mutmut_80 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_81'] = x_main__mutmut_81 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_82'] = x_main__mutmut_82 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_83'] = x_main__mutmut_83 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_84'] = x_main__mutmut_84 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_85'] = x_main__mutmut_85 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_86'] = x_main__mutmut_86 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_87'] = x_main__mutmut_87 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_88'] = x_main__mutmut_88 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_89'] = x_main__mutmut_89 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_90'] = x_main__mutmut_90 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_91'] = x_main__mutmut_91 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_92'] = x_main__mutmut_92 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_93'] = x_main__mutmut_93 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_94'] = x_main__mutmut_94 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_95'] = x_main__mutmut_95 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_96'] = x_main__mutmut_96 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_97'] = x_main__mutmut_97 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_98'] = x_main__mutmut_98 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_99'] = x_main__mutmut_99 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_100'] = x_main__mutmut_100 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_101'] = x_main__mutmut_101 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_102'] = x_main__mutmut_102 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_103'] = x_main__mutmut_103 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_104'] = x_main__mutmut_104 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_105'] = x_main__mutmut_105 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_106'] = x_main__mutmut_106 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_107'] = x_main__mutmut_107 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_108'] = x_main__mutmut_108 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_109'] = x_main__mutmut_109 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_110'] = x_main__mutmut_110 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_111'] = x_main__mutmut_111 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_112'] = x_main__mutmut_112 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_113'] = x_main__mutmut_113 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_114'] = x_main__mutmut_114 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_115'] = x_main__mutmut_115 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_116'] = x_main__mutmut_116 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_117'] = x_main__mutmut_117 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_118'] = x_main__mutmut_118 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_119'] = x_main__mutmut_119 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_120'] = x_main__mutmut_120 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_121'] = x_main__mutmut_121 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_122'] = x_main__mutmut_122 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_123'] = x_main__mutmut_123 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_124'] = x_main__mutmut_124 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_125'] = x_main__mutmut_125 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_126'] = x_main__mutmut_126 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_127'] = x_main__mutmut_127 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_128'] = x_main__mutmut_128 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_129'] = x_main__mutmut_129 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_130'] = x_main__mutmut_130 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_131'] = x_main__mutmut_131 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_132'] = x_main__mutmut_132 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_133'] = x_main__mutmut_133 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_134'] = x_main__mutmut_134 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_135'] = x_main__mutmut_135 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_136'] = x_main__mutmut_136 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_137'] = x_main__mutmut_137 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_138'] = x_main__mutmut_138 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_139'] = x_main__mutmut_139 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_140'] = x_main__mutmut_140 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_141'] = x_main__mutmut_141 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_142'] = x_main__mutmut_142 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_143'] = x_main__mutmut_143 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_144'] = x_main__mutmut_144 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_145'] = x_main__mutmut_145 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_146'] = x_main__mutmut_146 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_147'] = x_main__mutmut_147 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_148'] = x_main__mutmut_148 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_149'] = x_main__mutmut_149 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_150'] = x_main__mutmut_150 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_151'] = x_main__mutmut_151 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_152'] = x_main__mutmut_152 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_153'] = x_main__mutmut_153 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_154'] = x_main__mutmut_154 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_155'] = x_main__mutmut_155 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_156'] = x_main__mutmut_156 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_157'] = x_main__mutmut_157 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_158'] = x_main__mutmut_158 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_159'] = x_main__mutmut_159 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_160'] = x_main__mutmut_160 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_161'] = x_main__mutmut_161 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_162'] = x_main__mutmut_162 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_163'] = x_main__mutmut_163 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_164'] = x_main__mutmut_164 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_165'] = x_main__mutmut_165 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_166'] = x_main__mutmut_166 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_167'] = x_main__mutmut_167 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_168'] = x_main__mutmut_168 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_169'] = x_main__mutmut_169 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_170'] = x_main__mutmut_170 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_171'] = x_main__mutmut_171 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_172'] = x_main__mutmut_172 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_173'] = x_main__mutmut_173 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_174'] = x_main__mutmut_174 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_175'] = x_main__mutmut_175 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_176'] = x_main__mutmut_176 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_177'] = x_main__mutmut_177 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_178'] = x_main__mutmut_178 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_179'] = x_main__mutmut_179 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_180'] = x_main__mutmut_180 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_181'] = x_main__mutmut_181 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_182'] = x_main__mutmut_182 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_183'] = x_main__mutmut_183 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_184'] = x_main__mutmut_184 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_185'] = x_main__mutmut_185 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_186'] = x_main__mutmut_186 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_187'] = x_main__mutmut_187 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_188'] = x_main__mutmut_188 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_189'] = x_main__mutmut_189 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_190'] = x_main__mutmut_190 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_191'] = x_main__mutmut_191 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_192'] = x_main__mutmut_192 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_193'] = x_main__mutmut_193 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_194'] = x_main__mutmut_194 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_195'] = x_main__mutmut_195 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_196'] = x_main__mutmut_196 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_197'] = x_main__mutmut_197 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_198'] = x_main__mutmut_198 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_199'] = x_main__mutmut_199 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_200'] = x_main__mutmut_200 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_201'] = x_main__mutmut_201 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_202'] = x_main__mutmut_202 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_203'] = x_main__mutmut_203 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_204'] = x_main__mutmut_204 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_205'] = x_main__mutmut_205 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_206'] = x_main__mutmut_206 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_207'] = x_main__mutmut_207 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_208'] = x_main__mutmut_208 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_209'] = x_main__mutmut_209 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_210'] = x_main__mutmut_210 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_211'] = x_main__mutmut_211 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_212'] = x_main__mutmut_212 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_213'] = x_main__mutmut_213 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_214'] = x_main__mutmut_214 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_215'] = x_main__mutmut_215 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_216'] = x_main__mutmut_216 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_217'] = x_main__mutmut_217 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_218'] = x_main__mutmut_218 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_219'] = x_main__mutmut_219 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_220'] = x_main__mutmut_220 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_221'] = x_main__mutmut_221 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_222'] = x_main__mutmut_222 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_223'] = x_main__mutmut_223 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_224'] = x_main__mutmut_224 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_225'] = x_main__mutmut_225 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_226'] = x_main__mutmut_226 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_227'] = x_main__mutmut_227 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_228'] = x_main__mutmut_228 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_229'] = x_main__mutmut_229 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_230'] = x_main__mutmut_230 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_231'] = x_main__mutmut_231 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_232'] = x_main__mutmut_232 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_233'] = x_main__mutmut_233 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_234'] = x_main__mutmut_234 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_235'] = x_main__mutmut_235 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_236'] = x_main__mutmut_236 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_237'] = x_main__mutmut_237 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_238'] = x_main__mutmut_238 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_239'] = x_main__mutmut_239 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_240'] = x_main__mutmut_240 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_241'] = x_main__mutmut_241 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_242'] = x_main__mutmut_242 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_243'] = x_main__mutmut_243 # type: ignore # mutmut generated
